{
  "name": "@croco-calc/util",
  "exports": {
    "./*": {
      "types": "./src/*.ts",
      "import": "./dist/*.mjs",
      "require": "./dist/*.js"
    }
  },
  "scripts": {
    "dev": "tsup-node --watch",
    "build": "npm run madge && tsup-node",
    "test": "vitest run",
    "madge": " madge --circular --extensions ts ./src",
    "ts-check": "tsc --noEmit && tsc --noEmit -p ./__test__/tsconfig.json",
    "lint": "oxlint . --type-aware --type-check",
    "lint-fast": "oxlint ."
  },
  "devDependencies": {
    "@croco-calc/tsup-config": "workspace:*",
    "@croco-calc/typescript-config": "workspace:*",
    "@types/node": "24.9.1",
    "madge": "8.0.0",
    "oxlint": "1.75.0",
    "oxlint-tsgolint": "7.0.2001",
    "tsup": "8.4.0",
    "typescript": "7.0.2",
    "vitest": "4.1.0",
    "zod": "3.23.8"
  }
}
